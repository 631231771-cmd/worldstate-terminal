services:
  research-api:
    build:
      context: .
      dockerfile: services/research-api/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - worldstate-data:/data
    env_file:
      - path: .env
        required: false
    restart: unless-stopped

volumes:
  worldstate-data:
