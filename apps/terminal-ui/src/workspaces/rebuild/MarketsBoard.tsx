import { useMemo, useState } from "preact/hooks";
import type { ProductMarketItem } from "../../types/product";
import { Badge, DetailsDisclosure, Panel, Sparkline, StateMessage } from "../../components/Primitives";
const TABS = ["All", "Rates", "FX", "Equities", "Commodities", "Risk"] as const;
function group(item: ProductMarketItem): string { if (item.asset_class === "rates") return "Rates"; if (item.asset_class === "fx") return "FX"; if (item.asset_class === "equity_index") return "Equities"; if (item.asset_class === "metals" || item.asset_class === "energy") return "Commodities"; return "Risk"; }
function MarketRow({ item, onOpen }: { item: ProductMarketItem; onOpen: (item: ProductMarketItem) => void }) { const direction = item.direction === "up" ? "up" : item.direction === "down" ? "down" : "neutral"; return <tr class="market-row" onClick={() => onOpen(item)}><td><div class="market-row__name"><strong>{item.label}</strong></div></td><td class="market-row__value">{item.formatted_value}</td><td class={`market-row__change ${direction === "up" ? "positive" : direction === "down" ? "negative" : ""}`}>{item.change == null ? "—" : `${item.change > 0 ? "+" : ""}${item.change.toFixed(2)} ${item.change_unit}`}</td><td><Sparkline values={item.sparkline} tone={direction} /></td><td><span class={`status-dot status-dot--${item.status}`}>{item.status}</span></td></tr>; }
export function MarketsBoard({ items, onOpen }: { items: ProductMarketItem[]; onOpen: (item: ProductMarketItem) => void }) { const [tab, setTab] = useState<(typeof TABS)[number]>("All"); const filtered = useMemo(() => tab === "All" ? items : items.filter((item) => group(item) === tab), [items, tab]); if (!items.length) return <StateMessage title="No market data" detail="No observed daily market data satisfies the current product view." action={<button type="button" class="button-primary">Open Data Sources</button>} />; return <div class="workspace workspace--product"><section class="page-heading page-heading--compact"><div><div class="eyebrow">MARKETS / DAILY CONTEXT</div><h1>Market board</h1><p>Changes use the latest valid observations, not wall-clock weekend deltas.</p></div><div class="page-heading__aside"><Badge tone="good">{items.length} assets</Badge></div></section><div class="tabs-bar">{TABS.map((item) => <button type="button" class={tab === item ? "active" : ""} onClick={() => setTab(item)} key={item}>{item}</button>)}</div><Panel title="Cross-asset overview" eyebrow={tab.toUpperCase()}><div class="table-wrap"><table class="market-board"><thead><tr><th>Asset</th><th>Last</th><th>Change</th><th>Trend</th><th>Status</th></tr></thead><tbody>{filtered.map((item) => <MarketRow item={item} key={item.key} onOpen={onOpen} />)}</tbody></table></div>{!filtered.length ? <div class="empty-action"><div><h3>No observed data in this group</h3><p>Do not fill this view with fixture values.</p></div></div> : null}</Panel><Panel title="Reading notes" eyebrow="RESEARCH NOTE"><ul class="boundary-list"><li>Rates show basis points; FX, indices, and commodities show percentage change.</li><li>Provider, PIT, and quality details are progressively disclosed.</li><li>Daily context cannot replace minute event reaction data.</li></ul><DetailsDisclosure label="Capability boundary"><p class="method-note">Event research is enabled only when capability inventory reports eligible intraday data.</p></DetailsDisclosure></Panel></div>; }
