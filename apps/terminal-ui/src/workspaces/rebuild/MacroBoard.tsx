import { useState } from "preact/hooks";
import type { ProductCountry, ProductTodayResponse } from "../../types/product";
import { Badge, DetailsDisclosure, Panel } from "../../components/Primitives";
const DIMENSIONS = ["Growth", "Inflation", "Liquidity", "Policy", "Credit", "Risk"];
function CountryCell({ country, dimension, onOpen }: { country: ProductCountry; dimension: string; onOpen: () => void }) { const value = country.dimensions[dimension]; return <button type="button" class="matrix-cell" onClick={onOpen}><strong>{value?.score == null ? "—" : value.score.toFixed(2)}</strong><span>{value?.score == null ? "Missing" : value.direction}</span></button>; }
export function MacroBoard({ countries, onOpenCountry }: { countries: ProductCountry[]; onOpenCountry: (country: ProductCountry) => void }) { const [selected, setSelected] = useState<ProductCountry | null>(null); const active = selected ?? countries[0] ?? null; return <div class="workspace workspace--product"><section class="page-heading page-heading--compact"><div><div class="eyebrow">MACRO / GLOBAL MATRIX</div><h1>Macro matrix</h1><p>Only locally computed country dimensions are shown; missing data is not backfilled.</p></div><div class="page-heading__aside"><Badge tone="info">{countries.filter((item) => item.status === "available").length} countries with coverage</Badge></div></section><Panel title="Global state" eyebrow="GROWTH / INFLATION / POLICY / LIQUIDITY / CREDIT / RISK"><div class="table-wrap"><table class="matrix-table"><thead><tr><th>Country / region</th>{DIMENSIONS.map((dimension) => <th key={dimension}>{dimension}</th>)}</tr></thead><tbody>{countries.map((country) => <tr key={country.key}><td><button type="button" class="link-button" onClick={() => { setSelected(country); onOpenCountry(country); }}><strong>{country.label}</strong><small class="muted">{country.status}</small></button></td>{DIMENSIONS.map((dimension) => <td key={dimension}><CountryCell country={country} dimension={dimension} onOpen={() => { setSelected(country); onOpenCountry(country); }} /></td>)}</tr>)}</tbody></table></div></Panel>{active ? <Panel title={`${active.label} / current detail`} eyebrow="COUNTRY DETAIL" aside={<Badge tone={active.status === "available" ? "good" : active.status === "partial" ? "warn" : "neutral"}>{active.status}</Badge>}><div class="board-grid board-grid--markets">{Object.entries(active.dimensions).map(([dimension, value]) => <button type="button" class="metric-tile" key={dimension} onClick={() => onOpenCountry(active)}><span class="metric-tile__label">{dimension}</span><strong class="metric-tile__value">{value.score == null ? "—" : value.score.toFixed(2)}</strong><span class="metric-tile__meta">{value.direction} / {Math.round(value.confidence * 100)}%</span></button>)}</div><DetailsDisclosure label="Data boundaries"><p class="method-note">{active.details.limitations.join(" ") || "Computed from local Series and Observation data."}</p></DetailsDisclosure></Panel> : <Panel title="Select a country" eyebrow="COUNTRY DETAIL"><p class="muted">Click a matrix row to drill down.</p></Panel>}</div>; }
export function ProductMacroFromToday({ data, onOpenCountry }: { data: ProductTodayResponse; onOpenCountry: (country: ProductCountry) => void }) { return <MacroBoard countries={data.global} onOpenCountry={onOpenCountry} />; }
