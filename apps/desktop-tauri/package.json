{
  "name": "@worldstate/desktop",
  "private": true,
  "version": "0.7.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "node scripts/check-sidecar.cjs && tauri build",
    "check": "cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0"
  }
}
