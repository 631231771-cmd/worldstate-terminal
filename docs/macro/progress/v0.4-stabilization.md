# Macro Research Terminal v0.4 stabilization progress

Updated: 2026-08-02

Implemented locally:

- genuine point-in-time `surprise_z` gating and separate threshold scaling;
- NFP per-indicator revision scaling with no unsupported dominance claim;
- AnalysisRun-linked Regime matching, missing-dimension semantics and
  contamination policy;
- immutable release/consensus/stage/OHLCV/history/config/output manifests,
  idempotency and replay checks;
- per-claim Evidence ID validation and deterministic fallback;
- instrument calendar selection, tradable-minute coverage and explicit
  exchange-session-lite limitations;
- focused application import boundaries while retaining the compatibility
  facade for existing local callers;
- Tauri product health verification, foreign-port diagnostics, secret
  propagation and Rust unit tests;
- CI ordering that builds Terminal UI before Rust/Tauri validation;
- clean migration and method/API/UI regression tests.

Release gate:

- PR #8 remains Draft.
- Do not merge until all pushed GitHub Actions checks are green.
- No new macro event, workspace, automated trading or product-breadth work is
  included in this stabilization.

Still outside v0.4 readiness:

- licensed, sufficiently deep point-in-time consensus and minute history;
- a complete licensed futures calendar and product-specific settlement rules;
- frozen Python sidecar, signed Windows installer and blank-machine operation.
