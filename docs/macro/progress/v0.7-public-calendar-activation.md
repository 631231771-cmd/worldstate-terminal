# v0.7 public BLS calendar activation

Date: 2026-08-12  
Scope: automatic pre-T0 release scheduling only

## Implemented

`BlsOfficialProvider.fetch_schedule()` uses the official BLS public ICS feed
(`https://www.bls.gov/schedule/news_release/bls.ics`) for the current and
forward calendar.  It parses CPI and Employment Situation events, preserves
the title-supplied reference period, handles `America/New_York` DST, and
captures a durable source artifact with its hash and retrieval time.  The
official schedule HTML remains a secondary fallback and is labelled as such
in artifact and calendar-snapshot metadata.

`sync_bls_calendar` creates or updates an observed `MacroRelease` and its
`release` stage idempotently when the event contains a reference period.  An
ICS event without a period is retained only in the calendar snapshot; the
service does not invent a period label.

## Verification

- `tests/test_data_providers_v05.py` and `tests/test_bls_official_sync_v05.py`:
  31 passed.
- Ruff and strict mypy: passed.
- Runtime command attempted:
  `WorldState.bat sync-calendar --start-date 2026-08-12 --end-date 2026-08-12`.

## Current external blocker

On the runtime network, both official BLS endpoints returned HTTP 403.  The
service reports `provider_public_calendar_unavailable`, rather than treating
the public calendar as a BLS API entitlement.  The runtime therefore contains
zero observed CPI rows for `2026-07`; no manual import was used.  Once an
allowed route can reach either official endpoint, rerunning the same command
will perform the normal observed upsert without code changes.

The automated request identity is now stable and contactable:
`WorldStateTerminal/0.7 (+https://github.com/631231771-cmd/worldstate-terminal)`.
One low-frequency verification request per endpoint at 03:57 UTC still
returned `403` with `content-type=text/html`; no retry loop was run.

Market-minute download remains deliberately disabled until after T0.  Consensus
capture is performed only after the official release identity exists.

## Browser-assisted fallback completed

The runtime network continued to return 403 for both BLS endpoints.  The
controlled browser therefore captured the official BLS August 2026 schedule
page.  The capture is stored with `acquisition_transport=browser_capture`,
`official_source=true`, `manual_user_entry=false`, and the page hash.  It
created the observed release `us_cpi-2026-07-observed` at `2026-08-12T12:30Z`.
Repeating the capture reconciles to the same release and writes no duplicate.

Before T0, four Trading Economics pages were read in the controlled browser:
headline/core MoM and YoY.  The page tables explicitly distinguish
`Forecast` (survey consensus) from `TEForecast` (proprietary forecast).  Four
observed consensus rows were saved with `captured_at < T0`; TEForecast remains
separate and is never used as consensus.  This browser path is not claimed as
Trading Economics API access and does not grant API entitlement.
