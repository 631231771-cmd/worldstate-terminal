# v0.4 stabilization baseline

Recorded before the v0.4 stabilization changes on 2026-07-31 (Asia/Shanghai).

## Scope lock

- Branch: `refactor/macro-research-terminal`
- HEAD: `91eb7c8566d88495d0e3d06a6937b08d499945f5`
- Pull request: #8, open as Draft; it must remain Draft until all required checks pass.
- The `world-monitor-legacy-freeze` tag and `archive/world-monitor-legacy` branch are archival boundaries and are not modified by this work.

## CI baseline

| Check | Baseline result |
| --- | --- |
| Research API | success |
| Terminal UI | success |
| Desktop check | failure |

The desktop failure is reproducible: Tauri's `frontendDist` points to
`apps/terminal-ui/dist`, but the Windows job installed dependencies without
building the UI. The failing run was `30601105384`, job `91063748881`.

## Data and test baseline

- Alembic revision: `0003_macro_research_terminal`
- Collected Python tests: 19
- Coverage observed during collection: 43.72% (below the then configured 65% gate).
- API health snapshot: `worldstate-research-api`, API `v2`, methodology
  `macro-event-engine-v3`, product version `0.3.0`.

## Demonstration snapshots

The seeded demonstrations at baseline were:

| Release | ID | Baseline note |
| --- | --- | --- |
| CPI | `06691926-c9ba-4d6d-8dac-e1af716ee4a0` | classified as comprehensive hot surprise |
| NFP | `b6c22f9c-35db-5b55-8320-d6bcfc46306d` | classified as revision-led using the old, unit-mixing method |
| FOMC | `e7b140dc-9ef4-51b4-8769-fd1ed5a99e46` | classified as overall dovish, fixture data |

The UI snapshot could not be taken in this environment because the in-app
browser blocks direct local-loopback access. Structured API output and the
fixture records above are the reproducible baseline instead.

## Known baseline limitations being addressed

- Threshold scaling was exposed as a standardized surprise when historical
  forecast-error samples were insufficient.
- Historical matching did not consistently read the persisted regime snapshot.
- Analysis runs did not preserve an immutable input/output manifest.
- Session coverage used elapsed time rather than tradable minutes.
- Evidence validation was mostly textual rather than claim-by-claim.
- The desktop launcher treated an open port as a healthy WorldState backend.
